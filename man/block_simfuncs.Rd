% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simfunction_block.R,
%   R/simfunction_block_utils.R
\name{block_simfuncs}
\alias{block_simfuncs}
\alias{block_rNormalGLM}
\alias{block_rNormalReg}
\alias{block_rNormalGLM_update}
\alias{block_rNormalReg_update}
\alias{normalize_block}
\title{Conditionally independent block simulation (Gibbs / product likelihood)}
\usage{
block_rNormalGLM(
  n,
  y,
  x,
  block,
  prior_list = NULL,
  prior_lists = NULL,
  offset = NULL,
  weights = 1,
  family = gaussian(),
  Gridtype = 2L,
  n_envopt = NULL,
  use_parallel = TRUE,
  use_opencl = FALSE,
  verbose = FALSE,
  progbar = FALSE
)

block_rNormalReg(
  n,
  y,
  x,
  block,
  prior_list = NULL,
  prior_lists = NULL,
  offset = NULL,
  weights = 1,
  Gridtype = 2L
)

block_rNormalReg_update(
  mu_all,
  P = NULL,
  dispersion = NULL,
  y,
  x,
  block,
  prior_list = NULL,
  prior_lists = NULL,
  offset = NULL,
  weights = 1,
  Gridtype = 2L,
  coef_cols = NULL
)

block_rNormalGLM_update(
  mu_all,
  sigma_theta_sq = NULL,
  y,
  x,
  block,
  family = poisson(),
  prior_list = NULL,
  prior_lists = NULL,
  offset = NULL,
  weights = 1,
  Gridtype = 2L,
  n_envopt = 1L,
  use_parallel = TRUE,
  use_opencl = FALSE,
  verbose = FALSE,
  progbar = FALSE,
  theta_coef_col = 1L
)

normalize_block(block, l2)
}
\arguments{
\item{n}{Number of iid draws per block (\code{n = 1} typical for Gibbs).}

\item{y}{Response vector of length \code{nrow(x)}.}

\item{x}{Design matrix \code{nrow(x)} by \code{ncol(x)}; same \code{ncol} in every block.}

\item{block}{Block partition: \code{factor} or integer vector of length
\code{l2}, \code{l2_blocks} counts summing to \code{l2}, or a list of
disjoint row-index vectors covering \code{1:l2}.}

\item{prior_list}{Single prior specification recycled to all blocks, or with
\code{mu} as \code{l1} by \code{k} matrix or \code{blocks} sublist.}

\item{prior_lists}{Optional list of length \code{k} (or \code{1}) of per-block
\code{prior_list} objects.}

\item{offset}{Optional numeric vector (length \code{1} or \code{length(y)});
partitioned across blocks like \code{y}.}

\item{weights}{Optional weights; same recycling and blocking as \code{offset}.}

\item{family}{GLM \code{\link{family}} (not \code{gaussian()}).}

\item{Gridtype}{Passed to each block's sampler (Armadillo Gridtype).}

\item{n_envopt}{Passed to each block; defaults to \code{1} when \code{NULL}.}

\item{use_parallel, use_opencl, verbose, progbar}{Passed to each block's GLM sampler.}

\item{mu_all}{Numeric matrix \code{l1 x k} or vector of length \code{l1}
(recycled to all blocks): prior means per block (e.g. \eqn{X_\text{hyper}
  \gamma}). Required unless \code{prior_lists} or \code{prior_list} is
supplied.}

\item{P}{Prior precision matrix \code{l1 x l1} (inverse of
\eqn{\Sigma_{\text{ranef}}}). Shared across all blocks when building
per-block priors from \code{mu_all}.}

\item{dispersion}{Residual variance \eqn{\sigma^2}. Shared across all blocks
when building per-block priors from \code{mu_all}.}

\item{coef_cols}{Column indices of \code{coefficients} to return as
\code{b_draws} (default \code{NULL} returns all columns).}

\item{sigma_theta_sq}{Shared prior variance for scalar blocks when building
\code{prior_lists} from \code{mu_all}.}

\item{theta_coef_col}{Column index of \code{coefficients} to return as
\code{theta} (default \code{1} for scalar intercept blocks).}

\item{l2}{Number of observations (rows) after \code{model.frame}.}
}
\value{
A list with class \code{"block_rNormalGLM"} including:
\describe{
\item{coefficients}{Matrix \code{k * p}; row \code{b} is the draw for block \code{b}.}
\item{coef.mode}{Matrix \code{k * p}; posterior mode per block.}
\item{block_info}{Block partition metadata.}
\item{block_results}{List of length \code{k} with each block's sampler output.}
}

A list with class \code{"block_rNormalReg"} including
\code{coefficients}, \code{coef.mode}, \code{dispersion}, and
\code{block_info}.

A list with \code{b_draws}, \code{coefficients}, \code{coef.mode},
and \code{block_rNormalReg} (full block sampler output).

A list with \code{theta}, \code{coefficients}, \code{coef.mode}, and
\code{block_rNormalGLM} (full block sampler output).

List with \code{k}, \code{ids}, \code{l2_blocks}, \code{starts},
and \code{rows} (per-block row indices).
}
\description{
Draw from blockwise full conditionals when the posterior factorizes across
observation blocks.  \code{\link{block_rNormalReg}} uses
\code{.block_rNormalReg_cpp()} (Gaussian; each block calls \code{rNormalReg}).
\code{\link{block_rNormalGLM}} uses \code{.block_rNormalGLM_cpp()} (GLM envelope;
C++ partition and prior payload; each block calls \code{rNormalGLM}).  Typical use is \strong{block Gibbs}
(\code{n = 1} per outer step); \code{n > 1} gives iid draws from the product
conditional.
}
\details{
\strong{Output layout:} \code{coefficients} and \code{coef.mode} are matrices with
\strong{rows = blocks} and \strong{columns = predictors}.

See \code{inst/DESIGN_RGLM_BLOCKS.md}.

\strong{Per-block prior mean:} pass \code{prior_list$mu} as an \code{l1 x k}
matrix (one column per block).  Standard for Block~1 of the lmebayes Gibbs
sampler, where \eqn{\mu_j = X_{\text{hyper}} \gamma} depends on the current
fixed-effects draw.

\strong{Dispersion:} must be supplied via \code{prior_list$dispersion} (residual
variance \eqn{\sigma^2}).
}
\section{Functions}{
\itemize{
\item \code{block_rNormalReg()}: Gaussian blockwise full conditionals via
\code{.block_rNormalReg_cpp()} (C++ partition, prior payload, and
\code{rNormalReg()} per block).
This is the Gaussian counterpart of \code{\link{block_rNormalGLM}}.

\item \code{block_rNormalReg_update()}: One Gibbs block update via \code{block_rNormalReg}:
draw a single blockwise Gaussian posterior sample (\code{n = 1}) and return
coefficient draws for two-block Gibbs samplers where each group is its own
block (e.g. school-level random effects \eqn{b_j}). When \code{prior_lists}
and \code{prior_list} are both omitted, per-block \code{dNormal} priors are
built from \code{mu_all}, \code{P}, and \code{dispersion}.

\item \code{block_rNormalGLM_update()}: One Gibbs block update via \code{block_rNormalGLM}:
draw a single blockwise GLM posterior sample (\code{n = 1}) and return
latent-level draws (e.g. updated \code{theta}) for two-block Gibbs samplers
where each observation or group is its own block. When \code{prior_lists}
and \code{prior_list} are both omitted, per-block \code{dNormal} priors are
built from \code{mu_all} and \code{sigma_theta_sq}.

\item \code{normalize_block()}: Normalize a row-block partition for BY-style fits
and block Gibbs samplers.

}}
\seealso{
\code{\link{rNormal_reg}}, \code{\link{simfunction}},
\code{\link{normalize_block}}
}
\concept{block_simfuncs}
